import 'package:flutter/material.dart';

class AppGamesPage extends StatelessWidget {
  const AppGamesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
